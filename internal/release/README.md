# internal/release

Download e verificação de artefatos remotos: consulta `release.json` no GitHub para comparar versões, baixa `simulador.jar` e JRE somente quando necessário, e valida checksum SHA-256.
