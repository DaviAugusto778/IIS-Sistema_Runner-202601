name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

permissions:
  contents: read

jobs:
  build:
    name: build (${{ matrix.os }})
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, windows-latest]

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup Go
        uses: actions/setup-go@v5
        with:
          go-version: '1.25'
          cache: true

      - name: Setup Java 21
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '21'
          cache: maven

      - name: Make mvnw executable (Linux)
        if: runner.os == 'Linux'
        run: chmod +x assinador/mvnw

      - name: Build assinador.jar
        shell: bash
        working-directory: assinador
        run: |
          if [[ "${{ runner.os }}" == "Windows" ]]; then
            ./mvnw.cmd -B -ntp package
          else
            ./mvnw -B -ntp package
          fi

      - name: go vet
        run: go vet ./...

      - name: go test
        run: go test -race -count=1 ./...

      - name: Build CLI assinatura
        shell: bash
        run: |
          VERSION="$(git describe --tags --always --dirty)"
          COMMIT="$(git rev-parse --short HEAD)"
          DATE="$(date -u +%Y-%m-%dT%H:%M:%SZ)"
          LDFLAGS="-s -w \
            -X github.com/DaviAugusto778/IIS-Sistema_Runner-202601/cmd.Version=${VERSION} \
            -X github.com/DaviAugusto778/IIS-Sistema_Runner-202601/cmd.Commit=${COMMIT} \
            -X github.com/DaviAugusto778/IIS-Sistema_Runner-202601/cmd.Date=${DATE}"
          go build -ldflags "${LDFLAGS}" -o assinatura${{ runner.os == 'Windows' && '.exe' || '' }} ./

      - name: Smoke test --version
        shell: bash
        run: ./assinatura${{ runner.os == 'Windows' && '.exe' || '' }} version

      - name: Smoke test UTF-8 (criterio D)
        shell: bash
        run: |
          cp assinador/target/assinador.jar .
          out=$(./assinatura${{ runner.os == 'Windows' && '.exe' || '' }} sign 2>&1 || true)
          echo "$out"
          if ! echo "$out" | grep -q "Parâmetro"; then
            echo "ERRO: saida sem acentos UTF-8 — encoding quebrado"
            exit 1
          fi

      - name: Build CLI simulador
        shell: bash
        run: |
          VERSION="$(git describe --tags --always --dirty)"
          COMMIT="$(git rev-parse --short HEAD)"
          DATE="$(date -u +%Y-%m-%dT%H:%M:%SZ)"
          LDFLAGS="-s -w \
            -X main.Version=${VERSION} \
            -X main.Commit=${COMMIT} \
            -X main.Date=${DATE}"
          go build -ldflags "${LDFLAGS}" -o simulador${{ runner.os == 'Windows' && '.exe' || '' }} ./cmd/simulador

      - name: Smoke test simulador version + status
        shell: bash
        run: |
          ./simulador${{ runner.os == 'Windows' && '.exe' || '' }} version
          # status sem state file deve sair com codigo 1 e mensagem clara
          out=$(./simulador${{ runner.os == 'Windows' && '.exe' || '' }} status 2>&1 || true)
          echo "$out"
          if ! echo "$out" | grep -q "parado"; then
            echo "ERRO: status sem state deveria reportar 'parado'"
            exit 1
          fi

  lint:
    name: lint
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Go
        uses: actions/setup-go@v5
        with:
          go-version: '1.25'

      - name: golangci-lint
        uses: golangci/golangci-lint-action@v8
        with:
          version: v2.12.2
          args: --timeout=5m
