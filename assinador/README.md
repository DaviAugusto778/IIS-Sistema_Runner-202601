# assinador

Projeto Java 21 + Maven do `assinador.jar`. Contém a lógica de simulação de assinatura digital, validação de parâmetros e, na Sprint 3, o servidor HTTP com endpoints `/sign` e `/validate`.
